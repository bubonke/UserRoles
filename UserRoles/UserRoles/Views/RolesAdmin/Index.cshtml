@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
<br/>
<b/>

<div class="container col-lg-12" style="background-color:rgba(0,0,0,0.5)">
    <br />
    <br />
    <p style="text-decoration:none" class="btn btn-success">
        @Html.ActionLink("ADD Role", "Create")
    </p>
    <br />
    <br />
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>

            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <div style="text-decoration:none" class="btn btn-success">
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id })
                    </div> |
                    <div style="text-decoration:none" class="btn btn-info">
                        @Html.ActionLink("Details", "Details", new { id = item.Id })
                    </div>|
                    @*<div style="text-decoration:none" class="btn btn-danger btn-sm">
                            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                        </div>*@
                </td>
            </tr>
        }

    </table>


